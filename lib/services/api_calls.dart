import 'package:http/http.dart' as http;
import 'package:working_app/services/api_services.dart';

// https://reqres.in/api/users

const String _baseUrl = 'https://reqres.in/api/';

class FatchAPI {
  // getUserdata() async {
  //   var data = await ApiServices().getUsers('${_baseUrl}users');
  //   return data;
  // }
}
